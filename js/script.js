/* JavaScript handling */

//$(document).ready();
$(document).ready(function() {	// selection modification

	// var closeWidget = $("<a class='close'>Close</a>"); 
	// var toggleWidget = $("<a class='toggle-anchor' href=''>Toggle Sites Drawer</a>"); 
	Disable for now because the styling of this "CLOSE" is really annoying.
	$('#toggle-panel .t-wrap').append(closeWidget);
	// $('#header').prepend(toggleWidget);
	
	// $("#toggle-panel").hide();
	
	// $("#toggle-panel .close, a.toggle-anchor").click(function(){
			// $("#toggle-panel").slideToggle("fast","swing");
			// return false;
	// });
	
});



